---
description: Activate 1000xraw dashboard — Utopia Raw clean CPG brand
---

# 1000xraw

**Activate the 1000xraw project agent for Utopia Raw.**

Utopia Raw is Tyler's clean body-and-hair CPG brand. First product: body wash. Tagline: **Find Your Utopia.** Second Utopia Suite business after Utopia Browser. Highest priority per operating principle #6 in 1000xagent's CLAUDE.md.

## Dashboard

- **Repository:** `/home/kxdev/dev/utopia-raw`
- **Remote:** https://github.com/tyleryouk/utopia-raw.git
- **Domain:** `utopiaraw.com` (Porkbun, registered 2026-04-15)
- **Production:** not yet deployed
- **Manufacturer (Phase 0):** ONOXA (St Petersburg FL, 12-unit MOQ body wash)
- **Phase:** 0 — pre-manufacturer-outreach
- **Business:** `utopia-raw` (consumer-business, Utopia Suite)

## Load Context

1. Read `.claude/CLAUDE.md`
2. List `.claude/rules/`
3. Read `branding/colors.json`
4. Read `branding/typography.json`
5. Check `git status` for dirty state or unpushed commits
6. Check `git log --oneline -5` for recent activity
7. Count SVGs in `branding/wordmarks/candidates/` and `branding/wordmarks/selected/`

## Report

```
1000xraw dashboard
━━━━━━━━━━━━━━━━━━━━━━━━━
Repository: /home/kxdev/dev/utopia-raw
Remote:     https://github.com/tyleryouk/utopia-raw.git
Domain:     utopiaraw.com (Porkbun)
Phase:      0 — pre-outreach
Status:     {clean | dirty | unpushed}
Recent:     {last commit}

Branding:
  Candidates:  {N} SVGs
  Selected:    {N} SVGs
  Colors:      4-token palette loaded
  Typography:  wordmark + Georgia + Inter

Next actions:
  {list from Phase 0 playbook — see research references below}
━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Available Commands (all repo-local)

- `/1000xraw` — this dashboard
- `/1000xraw:generate-wordmark` — generate SVG wordmark candidates via Recraft + fal.ai (future)
- `/1000xraw:deploy` — deploy storefront to Vercel (future, Phase 2)

## Active Phase 0 Playbook

Research lives at `~/dev/utopia/.claude/research/4-15/utopia-suite-05-cpg-today.md` (14-day playbook) and `~/dev/utopia/.claude/research/4-15/utopia-suite-05-cpg.md` (strategic reference). These are shared across the Utopia Suite parent (utopia repo), not in 1000xagent's global folder.

## Parent Agent

1000xagent (master agent at `~/.claude/`) manages this project agent. For system-wide operations (`push-all`, `inspect`, etc.), invoke 1000xagent from `/home/kxdev/` — not from here.
